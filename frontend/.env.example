# The browser API root is deliberately fixed to same-origin /api/v1.
# Vite proxies /api during local development; nginx proxies it in Docker.
# Do not add a browser-visible backend, Colab, tunnel, or bearer-token URL here.
